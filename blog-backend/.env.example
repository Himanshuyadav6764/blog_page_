# IMPORTANT: Never commit your actual credentials to GitHub!
# This is a template file. Copy it to .env and fill in your actual values.

# MongoDB Connection String
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/blogDB

# For MongoDB Atlas (replace with your actual connection string):
# MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/blogDB?retryWrites=true&w=majority

# Server Port (Railway will provide this automatically)
PORT=5000

# Node Environment
NODE_ENV=production

# Frontend URL (for CORS) - Set this to your deployed frontend URL
FRONTEND_URL=*
